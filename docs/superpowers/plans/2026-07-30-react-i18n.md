# React i18n Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add runtime i18n (react-i18next) to the React UI with Bulgarian default, plus Russian and English, a persisted language switcher, and a dynamic MUI locale.

**Architecture:** i18next is initialized once (side-effect import in `index.tsx`) with a localStorage detector that falls back to Bulgarian. Components consume `useTranslation()` and render `t('key')`. A `LanguageSwitcher` calls `i18n.changeLanguage()`; a theme wrapper maps the active language to the matching `@mui/material/locale` pack. The test environment is forced to English so the existing English-asserting suite passes unchanged.

**Tech Stack:** React 19, TypeScript, MUI 9, `i18next`, `react-i18next`, `i18next-browser-languagedetector`, Jest + React Testing Library (CRA / react-scripts).

**Working directory for all commands:** `ui/react/`

---

## File Structure

- Create `ui/react/src/i18n/index.ts` — i18next init + exported instance.
- Create `ui/react/src/i18n/locales/{bg,ru,en}.json` — translation resources.
- Create `ui/react/src/components/LanguageSwitcher.tsx` — globe menu, BG/РУ/EN.
- Create `ui/react/src/theme.ts` — base theme options + `muiLocaleFor(lang)` helper.
- Modify `ui/react/src/index.tsx` — import `./i18n`.
- Modify `ui/react/src/setupTests.ts` — force English in tests.
- Modify `ui/react/src/App.tsx` — dynamic MUI locale, place switcher, migrate strings.
- Modify all screen/dialog components under `ui/react/src/components/` — migrate strings.
- Create tests: `src/i18n/i18n.test.ts`, `src/components/LanguageSwitcher.test.tsx`.

---

### Task 1: i18n scaffolding + init

**Files:**
- Create: `ui/react/src/i18n/index.ts`
- Create: `ui/react/src/i18n/locales/bg.json`, `ru.json`, `en.json`
- Create: `ui/react/src/i18n/i18n.test.ts`
- Modify: `ui/react/src/index.tsx`
- Modify: `ui/react/src/setupTests.ts`

- [ ] **Step 1: Install dependencies**

Run:
```bash
npm install i18next react-i18next i18next-browser-languagedetector
```
Expected: three packages added to `package.json` dependencies.

- [ ] **Step 2: Seed locale files with the first keys**

Create `src/i18n/locales/en.json`:
```json
{ "signIn": { "button": "Sign in" } }
```
Create `src/i18n/locales/bg.json`:
```json
{ "signIn": { "button": "Вход" } }
```
Create `src/i18n/locales/ru.json`:
```json
{ "signIn": { "button": "Войти" } }
```
(These grow to the full set in Task 5. Keep them valid JSON at every step.)

- [ ] **Step 3: Write the i18n init module**

Create `src/i18n/index.ts`:
```ts
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import LanguageDetector from 'i18next-browser-languagedetector';
import bg from './locales/bg.json';
import ru from './locales/ru.json';
import en from './locales/en.json';

export const SUPPORTED_LANGS = ['bg', 'ru', 'en'] as const;
export type Lang = (typeof SUPPORTED_LANGS)[number];
export const LANG_STORAGE_KEY = 'harmonia.lang';

i18n
  .use(LanguageDetector)
  .use(initReactI18next)
  .init({
    resources: {
      bg: { translation: bg },
      ru: { translation: ru },
      en: { translation: en },
    },
    fallbackLng: 'bg',
    supportedLngs: SUPPORTED_LANGS as unknown as string[],
    detection: {
      order: ['localStorage'],
      caches: ['localStorage'],
      lookupLocalStorage: LANG_STORAGE_KEY,
    },
    interpolation: { escapeValue: false },
    returnNull: false,
  });

export default i18n;
```

- [ ] **Step 4: Wire init into the app entrypoint**

In `src/index.tsx`, add the side-effect import directly after the existing `import './index.css';` line:
```ts
import './i18n';
```

- [ ] **Step 5: Force English in the test environment**

In `src/setupTests.ts`, append:
```ts
// i18n: run the suite in English so existing English-text assertions hold.
// Real users default to Bulgarian (see src/i18n/index.ts).
import i18n from './i18n';
beforeAll(async () => {
  await i18n.changeLanguage('en');
});
```

- [ ] **Step 6: Write the failing i18n test**

Create `src/i18n/i18n.test.ts`:
```ts
import i18n, { LANG_STORAGE_KEY, SUPPORTED_LANGS } from './index';

describe('i18n', () => {
  it('supports exactly bg, ru, en', () => {
    expect([...SUPPORTED_LANGS]).toEqual(['bg', 'ru', 'en']);
  });

  it('falls back to Bulgarian', () => {
    expect(i18n.options.fallbackLng).toContain('bg');
  });

  it('translates a known key in each language', () => {
    i18n.changeLanguage('en');
    expect(i18n.t('signIn.button')).toBe('Sign in');
    i18n.changeLanguage('bg');
    expect(i18n.t('signIn.button')).toBe('Вход');
    i18n.changeLanguage('ru');
    expect(i18n.t('signIn.button')).toBe('Войти');
    i18n.changeLanguage('en');
  });

  it('persists language choice to localStorage', () => {
    i18n.changeLanguage('ru');
    expect(localStorage.getItem(LANG_STORAGE_KEY)).toBe('ru');
    i18n.changeLanguage('en');
  });
});
```

- [ ] **Step 7: Run tests**

Run: `CI=true npx react-scripts test i18n.test --watchAll=false`
Expected: 4 tests pass.

- [ ] **Step 8: Commit**

```bash
git add ui/react/src/i18n ui/react/src/index.tsx ui/react/src/setupTests.ts ui/react/package.json ui/react/package-lock.json
git commit -m "feat(react): add i18next scaffolding (bg default, ru, en)"
```

---

### Task 2: Base theme + dynamic MUI locale

**Files:**
- Create: `ui/react/src/theme.ts`
- Test: covered via `App` render in later tasks (no standalone test needed here).

- [ ] **Step 1: Extract base theme and MUI-locale helper**

Create `src/theme.ts`:
```ts
import { createTheme, Theme } from '@mui/material';
import { bgBG, ruRU, enUS, Localization } from '@mui/material/locale';

const baseOptions = {
  palette: {
    primary: { main: '#2e6b4f' },
    background: { default: '#f5f5f0' },
  },
  shape: { borderRadius: 8 },
  typography: { fontFamily: 'system-ui, -apple-system, sans-serif' },
};

function muiLocaleFor(lang: string): Localization {
  if (lang.startsWith('ru')) return ruRU;
  if (lang.startsWith('en')) return enUS;
  return bgBG;
}

export function makeTheme(lang: string): Theme {
  return createTheme(baseOptions, muiLocaleFor(lang));
}
```

- [ ] **Step 2: Type-check**

Run: `npx tsc --noEmit`
Expected: exit 0.

- [ ] **Step 3: Commit**

```bash
git add ui/react/src/theme.ts
git commit -m "feat(react): base theme with per-language MUI locale helper"
```

---

### Task 3: LanguageSwitcher component

**Files:**
- Create: `ui/react/src/components/LanguageSwitcher.tsx`
- Create: `ui/react/src/components/LanguageSwitcher.test.tsx`

- [ ] **Step 1: Write the failing test**

Create `src/components/LanguageSwitcher.test.tsx`:
```tsx
import React from 'react';
import { render, screen, fireEvent, within } from '@testing-library/react';
import LanguageSwitcher from './LanguageSwitcher';
import i18n, { LANG_STORAGE_KEY } from '../i18n';

afterEach(() => { i18n.changeLanguage('en'); });

test('opens the menu and lists all three languages', () => {
  render(<LanguageSwitcher />);
  fireEvent.click(screen.getByLabelText(/language/i));
  const menu = screen.getByRole('menu');
  expect(within(menu).getByText('Български')).toBeInTheDocument();
  expect(within(menu).getByText('Русский')).toBeInTheDocument();
  expect(within(menu).getByText('English')).toBeInTheDocument();
});

test('selecting a language changes i18n and persists it', () => {
  render(<LanguageSwitcher />);
  fireEvent.click(screen.getByLabelText(/language/i));
  fireEvent.click(within(screen.getByRole('menu')).getByText('Русский'));
  expect(i18n.language).toBe('ru');
  expect(localStorage.getItem(LANG_STORAGE_KEY)).toBe('ru');
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `CI=true npx react-scripts test LanguageSwitcher --watchAll=false`
Expected: FAIL (module not found).

- [ ] **Step 3: Implement the component**

Create `src/components/LanguageSwitcher.tsx`:
```tsx
import React, { useState } from 'react';
import { IconButton, Menu, MenuItem, ListItemText } from '@mui/material';
import LanguageIcon from '@mui/icons-material/Language';
import { useTranslation } from 'react-i18next';
import { SUPPORTED_LANGS, Lang } from '../i18n';

const LABELS: Record<Lang, string> = {
  bg: 'Български',
  ru: 'Русский',
  en: 'English',
};

interface Props { color?: string; }

const LanguageSwitcher: React.FC<Props> = ({ color }) => {
  const { i18n } = useTranslation();
  const [anchor, setAnchor] = useState<null | HTMLElement>(null);
  const current = (i18n.language.split('-')[0] as Lang) || 'bg';

  const choose = (lang: Lang) => {
    i18n.changeLanguage(lang);
    setAnchor(null);
  };

  return (
    <>
      <IconButton
        aria-label="Change language"
        onClick={(e) => setAnchor(e.currentTarget)}
        sx={{ color: color ?? 'inherit' }}
        size="small"
      >
        <LanguageIcon fontSize="small" />
      </IconButton>
      <Menu anchorEl={anchor} open={Boolean(anchor)} onClose={() => setAnchor(null)}>
        {SUPPORTED_LANGS.map((lang) => (
          <MenuItem key={lang} selected={lang === current} onClick={() => choose(lang)}>
            <ListItemText>{LABELS[lang]}</ListItemText>
          </MenuItem>
        ))}
      </Menu>
    </>
  );
};

export default LanguageSwitcher;
```

- [ ] **Step 4: Run test to verify it passes**

Run: `CI=true npx react-scripts test LanguageSwitcher --watchAll=false`
Expected: 2 tests pass.

- [ ] **Step 5: Commit**

```bash
git add ui/react/src/components/LanguageSwitcher.tsx ui/react/src/components/LanguageSwitcher.test.tsx
git commit -m "feat(react): LanguageSwitcher (globe menu, BG/RU/EN)"
```

---

### Task 4: App shell — dynamic theme, switcher placement, nav/signin strings

**Files:**
- Modify: `ui/react/src/App.tsx`
- Modify: `ui/react/src/setupTests.ts` (only if a render helper is needed — not expected)

- [ ] **Step 1: Add the full app-shell keys to all three locale files**

Merge these into `en.json`, `bg.json`, `ru.json` respectively (top-level keys `nav`, `app`, `signIn`, `common`). English values are verbatim from current code.

`en.json` additions:
```json
{
  "common": {
    "cancel": "Cancel", "save": "Save", "saving": "Saving…", "close": "Close",
    "retry": "Retry", "period": "Period", "amount": "Amount",
    "description": "Description", "date": "Date", "household": "Household",
    "householdRef": "Household Ref", "displayName": "Display Name",
    "phone": "Phone", "email": "Email", "notes": "Notes"
  },
  "nav": {
    "directory": "Directory", "reservations": "Reservations", "finance": "Finance",
    "expenses": "Expenses", "fees": "Fees", "payments": "Payments",
    "notifications": "Notifications", "privacy": "Privacy",
    "contactEdit": "Edit Contact", "adminPending": "Pending Users"
  },
  "app": {
    "brand": "Harmonia", "viewAs": "View as:", "roleResident": "Resident",
    "roleAdmin": "Admin", "signOut": "Sign out", "user": "User"
  },
  "signIn": {
    "title": "Residence Harmonia",
    "subtitle": "Sign in to access your residence portal.",
    "button": "Sign in"
  }
}
```

`bg.json` additions:
```json
{
  "common": {
    "cancel": "Отказ", "save": "Запази", "saving": "Запазване…", "close": "Затвори",
    "retry": "Опитай отново", "period": "Период", "amount": "Сума",
    "description": "Описание", "date": "Дата", "household": "Домакинство",
    "householdRef": "Домакинство (реф.)", "displayName": "Име за показване",
    "phone": "Телефон", "email": "Имейл", "notes": "Бележки"
  },
  "nav": {
    "directory": "Указател", "reservations": "Резервации", "finance": "Финанси",
    "expenses": "Разходи", "fees": "Такси", "payments": "Плащания",
    "notifications": "Известия", "privacy": "Поверителност",
    "contactEdit": "Редакция на контакт", "adminPending": "Чакащи потребители"
  },
  "app": {
    "brand": "Хармония", "viewAs": "Преглед като:", "roleResident": "Обитател",
    "roleAdmin": "Администратор", "signOut": "Изход", "user": "Потребител"
  },
  "signIn": {
    "title": "Резиденция Хармония",
    "subtitle": "Влезте, за да получите достъп до портала на вашата резиденция.",
    "button": "Вход"
  }
}
```

`ru.json` additions:
```json
{
  "common": {
    "cancel": "Отмена", "save": "Сохранить", "saving": "Сохранение…", "close": "Закрыть",
    "retry": "Повторить", "period": "Период", "amount": "Сумма",
    "description": "Описание", "date": "Дата", "household": "Домохозяйство",
    "householdRef": "Домохозяйство (реф.)", "displayName": "Отображаемое имя",
    "phone": "Телефон", "email": "Эл. почта", "notes": "Заметки"
  },
  "nav": {
    "directory": "Каталог", "reservations": "Бронирования", "finance": "Финансы",
    "expenses": "Расходы", "fees": "Взносы", "payments": "Платежи",
    "notifications": "Уведомления", "privacy": "Конфиденциальность",
    "contactEdit": "Изменить контакт", "adminPending": "Ожидающие пользователи"
  },
  "app": {
    "brand": "Хармония", "viewAs": "Просмотр как:", "roleResident": "Житель",
    "roleAdmin": "Администратор", "signOut": "Выход", "user": "Пользователь"
  },
  "signIn": {
    "title": "Резиденция Хармония",
    "subtitle": "Войдите, чтобы получить доступ к порталу вашей резиденции.",
    "button": "Войти"
  }
}
```

- [ ] **Step 2: Migrate `App.tsx` to `t()` + dynamic theme + switcher**

In `App.tsx`:
1. Remove the top-level `const theme = createTheme(..., bgBG)` block and the `createTheme`/`bgBG` imports. Import instead:
   ```ts
   import { CssBaseline, ThemeProvider } from '@mui/material';
   import { useTranslation } from 'react-i18next';
   import { makeTheme } from './theme';
   import LanguageSwitcher from './components/LanguageSwitcher';
   ```
2. Wrap the theme in a component that recomputes on language change. Replace the `App` function body's `ThemeProvider theme={theme}` with a `useMemo`:
   ```tsx
   function App() {
     const { i18n } = useTranslation();
     const theme = React.useMemo(() => makeTheme(i18n.language), [i18n.language]);
     return (
       <ThemeProvider theme={theme}>
         <CssBaseline />
         <ErrorBoundary>
           <AuthenticatedTemplate><AppStatusGate /></AuthenticatedTemplate>
           <UnauthenticatedTemplate><SignInPage /></UnauthenticatedTemplate>
         </ErrorBoundary>
       </ThemeProvider>
     );
   }
   ```
   (Add `import React` usage is already present.)
3. `SignInPage`: add `const { t } = useTranslation();`. Replace texts:
   - `Residence Harmonia` → `{t('signIn.title')}`
   - `Sign in to access your residence portal.` → `{t('signIn.subtitle')}`
   - `Sign in` → `{t('signIn.button')}`
   - Add `<LanguageSwitcher color="inherit" />` inside the top `Box` (e.g., above the `HomeIcon`).
4. `MainApp`: add `const { t } = useTranslation();`. Replace:
   - The ten `<Tab label="…">` with `label={t('nav.directory')}` … `label={t('nav.adminPending')}` in the existing order.
   - `View as:` → `{t('app.viewAs')}`; `<ToggleButton value="resident">Resident</ToggleButton>` → `{t('app.roleResident')}`; `Admin` → `{t('app.roleAdmin')}`.
   - `Sign out` → `{t('app.signOut')}`.
   - `?? 'User'` → `?? t('app.user')`.
   - Add `<LanguageSwitcher />` in the AppBar `Box` next to the display name / sign-out.
   - Replace the `Harmonia` brand `Typography` text with `{t('app.brand')}`.

- [ ] **Step 3: Type-check and run the full suite**

Run: `npx tsc --noEmit && CI=true npx react-scripts test --watchAll=false`
Expected: tsc exit 0; all suites pass (tests run in English → identical text).

- [ ] **Step 4: Commit**

```bash
git add ui/react/src/App.tsx ui/react/src/i18n/locales
git commit -m "feat(react): i18n app shell — nav, sign-in, role toggle, switcher, dynamic MUI locale"
```

---

### Task 5: Directory + contact dialogs

**Files:**
- Modify: `ui/react/src/components/DirectoryList.tsx`, `EditContactDialog.tsx`,
  `AdminEditDialog.tsx`, `MarkDepartedDialog.tsx`, `EraseContactDialog.tsx`,
  `EraseMyContactDialog.tsx`, `ErrorBoundary.tsx`
- Modify: `ui/react/src/i18n/locales/{en,bg,ru}.json`

- [ ] **Step 1: Add the `directory`, `dialog`, `errorBoundary` keys to all three locale files**

`en.json`:
```json
{
  "directory": {
    "title": "Member Directory",
    "adminSubtitle": "Admin view — all active residents including opted-out.",
    "residentSubtitle": "Showing residents who have shared their details.",
    "myProfile": "My Profile",
    "searchAdmin": "Search name, apartment, phone, email…",
    "searchResident": "Search residents…",
    "loading": "Loading directory…",
    "apartment": "Apartment", "name": "Name", "optOut": "Opt-out",
    "departed": "Departed", "optedOut": "Opted out", "active": "Active",
    "noResidents": "No residents found.",
    "tipEdit": "Edit", "tipDepart": "Mark Departed", "tipErase": "Erase contact data",
    "toastProfileUpdated": "Profile updated.",
    "toastResidentUpdated": "Resident updated.",
    "toastDeparted": "{{ref}} marked as departed.",
    "toastMyDataDeleted": "Your data has been permanently deleted.",
    "toastContactErased": "Data for {{ref}} has been permanently deleted.",
    "errApi": "Could not reach the Harmonia API. Is it running on port 5000?",
    "errSave": "Could not save. Please try again.",
    "errDepart": "Could not mark as departed. Please try again.",
    "errDeleteMine": "Could not delete your data. Please try again.",
    "errErase": "Could not erase contact data. Please try again."
  },
  "dialog": {
    "myProfileTitle": "My Profile",
    "nameHelp": "Your name as shown to neighbours",
    "phonePlaceholder": "+359 88 …",
    "emailPlaceholder": "you@example.com",
    "adminNotesPlaceholder": "Internal notes — not visible to resident",
    "eraseContactTitle": "Erase contact data?",
    "eraseMineTitle": "Delete my data?",
    "permanentlyDeleted": "permanently deleted",
    "erasing": "Erasing…", "eraseContact": "Erase contact",
    "deleting": "Deleting…", "deleteMine": "Delete my data",
    "markDepartedTitle": "Mark as Departed?",
    "marking": "Marking…", "markDeparted": "Mark Departed"
  },
  "errorBoundary": { "title": "Something went wrong", "reload": "Reload" }
}
```

`bg.json`:
```json
{
  "directory": {
    "title": "Указател на живущите",
    "adminSubtitle": "Административен изглед — всички активни живущи, включително отказалите се.",
    "residentSubtitle": "Показани са живущите, които са споделили данните си.",
    "myProfile": "Моят профил",
    "searchAdmin": "Търсене по име, апартамент, телефон, имейл…",
    "searchResident": "Търсене на живущи…",
    "loading": "Зареждане на указателя…",
    "apartment": "Апартамент", "name": "Име", "optOut": "Отказ",
    "departed": "Напуснал", "optedOut": "Отказал се", "active": "Активен",
    "noResidents": "Няма намерени живущи.",
    "tipEdit": "Редакция", "tipDepart": "Отбележи като напуснал", "tipErase": "Изтрий контактните данни",
    "toastProfileUpdated": "Профилът е обновен.",
    "toastResidentUpdated": "Живущият е обновен.",
    "toastDeparted": "{{ref}} е отбелязан като напуснал.",
    "toastMyDataDeleted": "Вашите данни бяха окончателно изтрити.",
    "toastContactErased": "Данните за {{ref}} бяха окончателно изтрити.",
    "errApi": "Няма връзка с Harmonia API. Работи ли на порт 5000?",
    "errSave": "Запазването е неуспешно. Опитайте отново.",
    "errDepart": "Отбелязването като напуснал е неуспешно. Опитайте отново.",
    "errDeleteMine": "Изтриването на данните е неуспешно. Опитайте отново.",
    "errErase": "Изтриването на контактните данни е неуспешно. Опитайте отново."
  },
  "dialog": {
    "myProfileTitle": "Моят профил",
    "nameHelp": "Вашето име, както го виждат съседите",
    "phonePlaceholder": "+359 88 …",
    "emailPlaceholder": "you@example.com",
    "adminNotesPlaceholder": "Вътрешни бележки — не се виждат от живущия",
    "eraseContactTitle": "Изтриване на контактните данни?",
    "eraseMineTitle": "Изтриване на моите данни?",
    "permanentlyDeleted": "окончателно изтрити",
    "erasing": "Изтриване…", "eraseContact": "Изтрий контакта",
    "deleting": "Изтриване…", "deleteMine": "Изтрий моите данни",
    "markDepartedTitle": "Отбелязване като напуснал?",
    "marking": "Отбелязване…", "markDeparted": "Отбележи като напуснал"
  },
  "errorBoundary": { "title": "Нещо се обърка", "reload": "Презареди" }
}
```

`ru.json`:
```json
{
  "directory": {
    "title": "Каталог жильцов",
    "adminSubtitle": "Просмотр администратора — все активные жильцы, включая отказавшихся.",
    "residentSubtitle": "Показаны жильцы, поделившиеся своими данными.",
    "myProfile": "Мой профиль",
    "searchAdmin": "Поиск по имени, квартире, телефону, почте…",
    "searchResident": "Поиск жильцов…",
    "loading": "Загрузка каталога…",
    "apartment": "Квартира", "name": "Имя", "optOut": "Отказ",
    "departed": "Выбыл", "optedOut": "Отказался", "active": "Активен",
    "noResidents": "Жильцы не найдены.",
    "tipEdit": "Изменить", "tipDepart": "Отметить выбывшим", "tipErase": "Удалить контактные данные",
    "toastProfileUpdated": "Профиль обновлён.",
    "toastResidentUpdated": "Жилец обновлён.",
    "toastDeparted": "{{ref}} отмечен как выбывший.",
    "toastMyDataDeleted": "Ваши данные безвозвратно удалены.",
    "toastContactErased": "Данные для {{ref}} безвозвратно удалены.",
    "errApi": "Нет связи с Harmonia API. Он запущен на порту 5000?",
    "errSave": "Не удалось сохранить. Попробуйте снова.",
    "errDepart": "Не удалось отметить выбывшим. Попробуйте снова.",
    "errDeleteMine": "Не удалось удалить ваши данные. Попробуйте снова.",
    "errErase": "Не удалось удалить контактные данные. Попробуйте снова."
  },
  "dialog": {
    "myProfileTitle": "Мой профиль",
    "nameHelp": "Ваше имя, как его видят соседи",
    "phonePlaceholder": "+359 88 …",
    "emailPlaceholder": "you@example.com",
    "adminNotesPlaceholder": "Внутренние заметки — не видны жильцу",
    "eraseContactTitle": "Удалить контактные данные?",
    "eraseMineTitle": "Удалить мои данные?",
    "permanentlyDeleted": "безвозвратно удалены",
    "erasing": "Удаление…", "eraseContact": "Удалить контакт",
    "deleting": "Удаление…", "deleteMine": "Удалить мои данные",
    "markDepartedTitle": "Отметить как выбывшего?",
    "marking": "Отметка…", "markDeparted": "Отметить выбывшим"
  },
  "errorBoundary": { "title": "Что-то пошло не так", "reload": "Перезагрузить" }
}
```

- [ ] **Step 2: Migrate the components**

For each component add `import { useTranslation } from 'react-i18next';` and `const { t } = useTranslation();` at the top of the component body, then replace strings:

- `DirectoryList.tsx`: `Member Directory`→`t('directory.title')`; the two subtitle strings→`t('directory.adminSubtitle')`/`t('directory.residentSubtitle')`; `My Profile`→`t('directory.myProfile')`; placeholders→`t('directory.searchAdmin')`/`t('directory.searchResident')`; `Retry`→`t('common.retry')`; `Loading directory…`→`t('directory.loading')`; column headers `Apartment/Name/Phone/Email/Opt-out/Departed`→`t('directory.apartment')`,`t('directory.name')`,`t('common.phone')`,`t('common.email')`,`t('directory.optOut')`,`t('directory.departed')`; chips `Opted out`/`Active`→`t('directory.optedOut')`/`t('directory.active')`; icon `title=` props→`t('directory.tipEdit')`,`t('directory.tipDepart')`,`t('directory.tipErase')`; `No residents found.`→`t('directory.noResidents')`; toasts/errors → the `directory.toast*`/`directory.err*` keys. For the two interpolated toasts use `t('directory.toastDeparted', { ref: departRef })` and `t('directory.toastContactErased', { ref: eraseContactRef })`.
- `EditContactDialog.tsx`: `My Profile`→`t('dialog.myProfileTitle')`; labels `Display Name/Phone/Email`→`t('common.displayName')`,`t('common.phone')`,`t('common.email')`; placeholders→`t('dialog.nameHelp')`,`t('dialog.phonePlaceholder')`,`t('dialog.emailPlaceholder')`; `Saving…`/`Save`→`t('common.saving')`/`t('common.save')`; add `Cancel`→`t('common.cancel')` if present.
- `AdminEditDialog.tsx`: labels `Display Name/Phone/Email/Notes`→`common.*`/`t('common.notes')`; placeholder→`t('dialog.adminNotesPlaceholder')`; `Cancel`→`t('common.cancel')`; `Saving…`/`Save`→`common.*`.
- `MarkDepartedDialog.tsx`: `Mark as Departed?`→`t('dialog.markDepartedTitle')`; `Cancel`→`t('common.cancel')`; `Marking…`/`Mark Departed`→`t('dialog.marking')`/`t('dialog.markDeparted')`.
- `EraseContactDialog.tsx`: `Erase contact data?`→`t('dialog.eraseContactTitle')`; `permanently deleted`→`t('dialog.permanentlyDeleted')`; `Cancel`→`t('common.cancel')`; `Erasing…`/`Erase contact`→`t('dialog.erasing')`/`t('dialog.eraseContact')`.
- `EraseMyContactDialog.tsx`: `Delete my data?`→`t('dialog.eraseMineTitle')`; `permanently deleted`→`t('dialog.permanentlyDeleted')`; `Cancel`→`t('common.cancel')`; `Deleting…`/`Delete my data`→`t('dialog.deleting')`/`t('dialog.deleteMine')`.
- `ErrorBoundary.tsx`: this is a class component. Add `import { withTranslation, WithTranslation } from 'react-i18next';`, extend props with `WithTranslation`, use `this.props.t('errorBoundary.title')` and `t('errorBoundary.reload')`, and `export default withTranslation()(ErrorBoundary);`. Update `App.tsx` import if the default export name is unchanged (it is).

- [ ] **Step 3: Type-check and run the full suite**

Run: `npx tsc --noEmit && CI=true npx react-scripts test --watchAll=false`
Expected: tsc exit 0; all suites pass (English text unchanged). If any dialog/directory test now queries a string that moved to an interpolated key, update the test to the same English text.

- [ ] **Step 4: Commit**

```bash
git add ui/react/src/components ui/react/src/i18n/locales
git commit -m "feat(react): i18n directory + contact dialogs + error boundary"
```

---

### Task 6: Finance cluster (Financial, Expenses, Fees, Payments)

**Files:**
- Modify: `ui/react/src/components/FinancialScreen.tsx`, `ExpensesScreen.tsx`,
  `MaintenanceFeesScreen.tsx`, `PaymentsScreen.tsx`
- Modify: `ui/react/src/i18n/locales/{en,bg,ru}.json`

- [ ] **Step 1: Add the `finance`, `expenses`, `fees`, `payments` keys to all three locale files**

`en.json`:
```json
{
  "finance": {
    "periodLabel": "Period:",
    "totalCharges": "Total charges this period",
    "totalExpenses": "Total expenses this period",
    "myCharges": "My Charges", "myPayments": "My Payments",
    "dateReceived": "Date received", "requestPayment": "Request Payment",
    "requestInfo": "Payments are recorded by the building administrator.",
    "errLoad": "Could not load financial data. Please try again."
  },
  "expenses": {
    "record": "Record Expense", "amountEuro": "Amount (€)", "expenseDate": "Expense date",
    "ledger": "Expense Ledger", "category": "Category", "recorded": "Expense recorded.",
    "chargedAt": "Charged At",
    "errLoad": "Could not load expenses. Please try again.",
    "errAmount": "Enter a valid amount greater than zero.",
    "errRecord": "Could not record expense. Please try again."
  },
  "fees": {
    "record": "Record Charge", "amountEuro": "Amount (€)", "periodYm": "Period (YYYY-MM)",
    "recorded": "Charge recorded.", "allCharges": "All Charges", "myCharges": "My Charges",
    "chargedAt": "Charged At",
    "errLoad": "Could not load charges. Please try again.",
    "errInput": "Enter a valid household ref and amount greater than zero.",
    "errRecord": "Could not record charge. Please try again."
  },
  "payments": {
    "record": "Record Payment", "amountEuro": "Amount (€)", "periodYm": "Period (YYYY-MM)",
    "dateReceived": "Date received", "recorded": "Payment recorded.",
    "charged": "Charged", "paid": "Paid", "balance": "Balance",
    "allPayments": "All Payments", "myPayments": "My Payments",
    "errLoad": "Could not load payments. Please try again.",
    "errInput": "Enter a valid household ref and amount greater than zero.",
    "errRecord": "Could not record payment. Please try again."
  }
}
```

`bg.json`:
```json
{
  "finance": {
    "periodLabel": "Период:",
    "totalCharges": "Общо начисления за периода",
    "totalExpenses": "Общо разходи за периода",
    "myCharges": "Моите начисления", "myPayments": "Моите плащания",
    "dateReceived": "Дата на получаване", "requestPayment": "Заяви плащане",
    "requestInfo": "Плащанията се вписват от администратора на сградата.",
    "errLoad": "Финансовите данни не се заредиха. Опитайте отново."
  },
  "expenses": {
    "record": "Впиши разход", "amountEuro": "Сума (€)", "expenseDate": "Дата на разхода",
    "ledger": "Дневник на разходите", "category": "Категория", "recorded": "Разходът е вписан.",
    "chargedAt": "Начислено на",
    "errLoad": "Разходите не се заредиха. Опитайте отново.",
    "errAmount": "Въведете валидна сума, по-голяма от нула.",
    "errRecord": "Разходът не беше вписан. Опитайте отново."
  },
  "fees": {
    "record": "Впиши начисление", "amountEuro": "Сума (€)", "periodYm": "Период (ГГГГ-ММ)",
    "recorded": "Начислението е вписано.", "allCharges": "Всички начисления", "myCharges": "Моите начисления",
    "chargedAt": "Начислено на",
    "errLoad": "Начисленията не се заредиха. Опитайте отново.",
    "errInput": "Въведете валидно домакинство и сума, по-голяма от нула.",
    "errRecord": "Начислението не беше вписано. Опитайте отново."
  },
  "payments": {
    "record": "Впиши плащане", "amountEuro": "Сума (€)", "periodYm": "Период (ГГГГ-ММ)",
    "dateReceived": "Дата на получаване", "recorded": "Плащането е вписано.",
    "charged": "Начислено", "paid": "Платено", "balance": "Баланс",
    "allPayments": "Всички плащания", "myPayments": "Моите плащания",
    "errLoad": "Плащанията не се заредиха. Опитайте отново.",
    "errInput": "Въведете валидно домакинство и сума, по-голяма от нула.",
    "errRecord": "Плащането не беше вписано. Опитайте отново."
  }
}
```

`ru.json`:
```json
{
  "finance": {
    "periodLabel": "Период:",
    "totalCharges": "Всего начислений за период",
    "totalExpenses": "Всего расходов за период",
    "myCharges": "Мои начисления", "myPayments": "Мои платежи",
    "dateReceived": "Дата получения", "requestPayment": "Запросить платёж",
    "requestInfo": "Платежи вносит администратор здания.",
    "errLoad": "Не удалось загрузить финансовые данные. Попробуйте снова."
  },
  "expenses": {
    "record": "Внести расход", "amountEuro": "Сумма (€)", "expenseDate": "Дата расхода",
    "ledger": "Журнал расходов", "category": "Категория", "recorded": "Расход внесён.",
    "chargedAt": "Начислено",
    "errLoad": "Не удалось загрузить расходы. Попробуйте снова.",
    "errAmount": "Введите корректную сумму больше нуля.",
    "errRecord": "Не удалось внести расход. Попробуйте снова."
  },
  "fees": {
    "record": "Внести начисление", "amountEuro": "Сумма (€)", "periodYm": "Период (ГГГГ-ММ)",
    "recorded": "Начисление внесено.", "allCharges": "Все начисления", "myCharges": "Мои начисления",
    "chargedAt": "Начислено",
    "errLoad": "Не удалось загрузить начисления. Попробуйте снова.",
    "errInput": "Введите корректное домохозяйство и сумму больше нуля.",
    "errRecord": "Не удалось внести начисление. Попробуйте снова."
  },
  "payments": {
    "record": "Внести платёж", "amountEuro": "Сумма (€)", "periodYm": "Период (ГГГГ-ММ)",
    "dateReceived": "Дата получения", "recorded": "Платёж внесён.",
    "charged": "Начислено", "paid": "Оплачено", "balance": "Баланс",
    "allPayments": "Все платежи", "myPayments": "Мои платежи",
    "errLoad": "Не удалось загрузить платежи. Попробуйте снова.",
    "errInput": "Введите корректное домохозяйство и сумму больше нуля.",
    "errRecord": "Не удалось внести платёж. Попробуйте снова."
  }
}
```

- [ ] **Step 2: Migrate the four components**

Add `useTranslation` to each. Replace using the keys above plus `common.*` for shared labels (`Date`→`t('common.date')`, `Description`→`t('common.description')`, `Amount`→`t('common.amount')`, `Period`→`t('common.period')`, `Household`→`t('common.household')`, `Household Ref`→`t('common.householdRef')`, `Retry`→`t('common.retry')`, `Close`→`t('common.close')`). For the role-conditional titles (`All Charges`/`My Charges`, `All Payments`/`My Payments`) select the key by role, e.g. `t(role === 'admin' ? 'fees.allCharges' : 'fees.myCharges')`.

- [ ] **Step 3: Type-check and run the full suite**

Run: `npx tsc --noEmit && CI=true npx react-scripts test --watchAll=false`
Expected: tsc exit 0; all suites pass. Update any finance test that asserts a moved string to the same English literal.

- [ ] **Step 4: Commit**

```bash
git add ui/react/src/components ui/react/src/i18n/locales
git commit -m "feat(react): i18n finance cluster (financial, expenses, fees, payments)"
```

---

### Task 7: Remaining screens (Notifications, Privacy, ContactEdit, AdminPending, Reservation, ResidentPending)

**Files:**
- Modify: `ui/react/src/components/NotificationsScreen.tsx`, `PrivacyScreen.tsx`,
  `ContactEditScreen.tsx`, `AdminPendingScreen.tsx`, `ReservationScreen.tsx`,
  `ResidentPendingScreen.tsx`
- Modify: `ui/react/src/i18n/locales/{en,bg,ru}.json`

- [ ] **Step 1: Add the remaining keys to all three locale files**

`en.json`:
```json
{
  "notifications": {
    "send": "Send Announcement", "title": "Title", "titlePlaceholder": "Announcement title",
    "body": "Body", "bodyPlaceholder": "Message body", "sent": "Announcement sent.",
    "history": "Notification History", "sentAt": "Sent", "channel": "Channel",
    "errLoad": "Could not load notifications. Please try again.",
    "errRequired": "Title and body are required.",
    "errSend": "Could not send announcement. Please try again."
  },
  "privacy": {
    "deleteMine": "Delete My Contact Data", "deletedMine": "Your contact data has been deleted.",
    "dsarTitle": "DSAR Contact Erasure", "eraseRefLabel": "Erase Household Ref",
    "departTitle": "Mark Household as Departed", "departRefLabel": "Depart Household Ref",
    "sweepTitle": "Annual Retention Sweep",
    "errDelete": "Could not delete contact data. Please try again.",
    "toastErased": "Contact erased.", "toastNotFound": "Contact not found.",
    "errErase": "Could not complete erasure. Please try again.",
    "toastDeparted": "Household marked as departed.", "toastDepartNotFound": "Household not found.",
    "errDepart": "Could not mark as departed. Please try again.",
    "errSweep": "Could not run retention sweep. Please try again."
  },
  "contactEdit": {
    "myTitle": "My Contact Details", "optOut": "Opt out of directory listing",
    "saved": "Contact details saved.", "updateContact": "Update Household Contact",
    "optedOut": "Opted out", "contactUpdated": "Contact updated.",
    "updateNotes": "Update Notes", "notesUpdated": "Notes updated.",
    "errSave": "Could not save contact details. Please try again.",
    "errUpdate": "Could not update contact. Please try again.",
    "errNotes": "Could not update notes. Please try again."
  },
  "adminPending": {
    "title": "Pending Sign-Ins", "accessRequired": "Admin access required to view pending sign-ins.",
    "oid": "OID", "firstSeen": "First Seen", "linkTitle": "Link to Household",
    "householdRefLabel": "Household Ref", "refPlaceholder": "e.g. AP-101",
    "purgeTitle": "Purge Expired Entries?",
    "errLoad": "Could not load pending sign-ins. Please try again.",
    "linked": "Linked successfully.", "alreadyLinked": "Already linked to a household.",
    "gone": "Pending entry no longer exists — it may have been removed.",
    "accessDenied": "Admin access required.", "failed": "Failed — please try again.",
    "purged": "Deleted {{count}} expired entries.", "noneExpired": "No expired entries found."
  },
  "reservation": {
    "selectDate": "Select date:", "free": "Free", "yours": "Yours", "taken": "Taken",
    "claiming": "Claiming…", "claim": "Claim",
    "errLoad": "Could not load slots. Check your connection and try again.",
    "errTaken": "Slot already taken by someone else.",
    "errConfirm": "Could not confirm booking. Please try again.",
    "errNetwork": "Network error. Please try again."
  }
}
```

`bg.json`:
```json
{
  "notifications": {
    "send": "Изпрати съобщение", "title": "Заглавие", "titlePlaceholder": "Заглавие на съобщението",
    "body": "Съдържание", "bodyPlaceholder": "Текст на съобщението", "sent": "Съобщението е изпратено.",
    "history": "История на известията", "sentAt": "Изпратено", "channel": "Канал",
    "errLoad": "Известията не се заредиха. Опитайте отново.",
    "errRequired": "Заглавието и съдържанието са задължителни.",
    "errSend": "Съобщението не беше изпратено. Опитайте отново."
  },
  "privacy": {
    "deleteMine": "Изтрий моите контактни данни", "deletedMine": "Вашите контактни данни бяха изтрити.",
    "dsarTitle": "Изтриване на контакт по DSAR", "eraseRefLabel": "Домакинство за изтриване",
    "departTitle": "Отбележи домакинство като напуснало", "departRefLabel": "Домакинство за напускане",
    "sweepTitle": "Годишно почистване на данните",
    "errDelete": "Контактните данни не бяха изтрити. Опитайте отново.",
    "toastErased": "Контактът е изтрит.", "toastNotFound": "Контактът не е намерен.",
    "errErase": "Изтриването не беше завършено. Опитайте отново.",
    "toastDeparted": "Домакинството е отбелязано като напуснало.", "toastDepartNotFound": "Домакинството не е намерено.",
    "errDepart": "Отбелязването като напуснало е неуспешно. Опитайте отново.",
    "errSweep": "Почистването на данните е неуспешно. Опитайте отново."
  },
  "contactEdit": {
    "myTitle": "Моите контактни данни", "optOut": "Откажи се от указателя",
    "saved": "Контактните данни са запазени.", "updateContact": "Обнови контакт на домакинство",
    "optedOut": "Отказал се", "contactUpdated": "Контактът е обновен.",
    "updateNotes": "Обнови бележки", "notesUpdated": "Бележките са обновени.",
    "errSave": "Контактните данни не бяха запазени. Опитайте отново.",
    "errUpdate": "Контактът не беше обновен. Опитайте отново.",
    "errNotes": "Бележките не бяха обновени. Опитайте отново."
  },
  "adminPending": {
    "title": "Чакащи вписвания", "accessRequired": "Необходим е администраторски достъп за преглед на чакащите вписвания.",
    "oid": "OID", "firstSeen": "Първо видяно", "linkTitle": "Свържи с домакинство",
    "householdRefLabel": "Домакинство (реф.)", "refPlaceholder": "напр. AP-101",
    "purgeTitle": "Изчистване на изтеклите записи?",
    "errLoad": "Чакащите вписвания не се заредиха. Опитайте отново.",
    "linked": "Свързването е успешно.", "alreadyLinked": "Вече е свързано с домакинство.",
    "gone": "Чакащото вписване вече не съществува — може да е премахнато.",
    "accessDenied": "Необходим е администраторски достъп.", "failed": "Неуспешно — опитайте отново.",
    "purged": "Изтрити са {{count}} изтекли записа.", "noneExpired": "Няма изтекли записи."
  },
  "reservation": {
    "selectDate": "Изберете дата:", "free": "Свободно", "yours": "Ваше", "taken": "Заето",
    "claiming": "Запазване…", "claim": "Запази",
    "errLoad": "Часовете не се заредиха. Проверете връзката и опитайте отново.",
    "errTaken": "Часът вече е зает от друг.",
    "errConfirm": "Резервацията не беше потвърдена. Опитайте отново.",
    "errNetwork": "Мрежова грешка. Опитайте отново."
  }
}
```

`ru.json`:
```json
{
  "notifications": {
    "send": "Отправить объявление", "title": "Заголовок", "titlePlaceholder": "Заголовок объявления",
    "body": "Текст", "bodyPlaceholder": "Текст сообщения", "sent": "Объявление отправлено.",
    "history": "История уведомлений", "sentAt": "Отправлено", "channel": "Канал",
    "errLoad": "Не удалось загрузить уведомления. Попробуйте снова.",
    "errRequired": "Заголовок и текст обязательны.",
    "errSend": "Не удалось отправить объявление. Попробуйте снова."
  },
  "privacy": {
    "deleteMine": "Удалить мои контактные данные", "deletedMine": "Ваши контактные данные удалены.",
    "dsarTitle": "Удаление контакта по DSAR", "eraseRefLabel": "Домохозяйство для удаления",
    "departTitle": "Отметить домохозяйство выбывшим", "departRefLabel": "Домохозяйство для выбытия",
    "sweepTitle": "Ежегодная очистка данных",
    "errDelete": "Не удалось удалить контактные данные. Попробуйте снова.",
    "toastErased": "Контакт удалён.", "toastNotFound": "Контакт не найден.",
    "errErase": "Не удалось завершить удаление. Попробуйте снова.",
    "toastDeparted": "Домохозяйство отмечено выбывшим.", "toastDepartNotFound": "Домохозяйство не найдено.",
    "errDepart": "Не удалось отметить выбывшим. Попробуйте снова.",
    "errSweep": "Не удалось выполнить очистку данных. Попробуйте снова."
  },
  "contactEdit": {
    "myTitle": "Мои контактные данные", "optOut": "Отказаться от каталога",
    "saved": "Контактные данные сохранены.", "updateContact": "Обновить контакт домохозяйства",
    "optedOut": "Отказался", "contactUpdated": "Контакт обновлён.",
    "updateNotes": "Обновить заметки", "notesUpdated": "Заметки обновлены.",
    "errSave": "Не удалось сохранить контактные данные. Попробуйте снова.",
    "errUpdate": "Не удалось обновить контакт. Попробуйте снова.",
    "errNotes": "Не удалось обновить заметки. Попробуйте снова."
  },
  "adminPending": {
    "title": "Ожидающие входы", "accessRequired": "Требуется доступ администратора для просмотра ожидающих входов.",
    "oid": "OID", "firstSeen": "Впервые замечено", "linkTitle": "Связать с домохозяйством",
    "householdRefLabel": "Домохозяйство (реф.)", "refPlaceholder": "напр. AP-101",
    "purgeTitle": "Очистить просроченные записи?",
    "errLoad": "Не удалось загрузить ожидающие входы. Попробуйте снова.",
    "linked": "Связано успешно.", "alreadyLinked": "Уже связано с домохозяйством.",
    "gone": "Ожидающая запись больше не существует — возможно, она удалена.",
    "accessDenied": "Требуется доступ администратора.", "failed": "Ошибка — попробуйте снова.",
    "purged": "Удалено записей: {{count}}.", "noneExpired": "Просроченных записей нет."
  },
  "reservation": {
    "selectDate": "Выберите дату:", "free": "Свободно", "yours": "Ваше", "taken": "Занято",
    "claiming": "Бронирование…", "claim": "Забронировать",
    "errLoad": "Не удалось загрузить слоты. Проверьте соединение и попробуйте снова.",
    "errTaken": "Слот уже занят кем-то другим.",
    "errConfirm": "Не удалось подтвердить бронь. Попробуйте снова.",
    "errNetwork": "Ошибка сети. Попробуйте снова."
  }
}
```

- [ ] **Step 2: Migrate the six components**

Add `useTranslation` to each and replace strings with the keys above plus `common.*` shared labels (`Cancel`, `Retry`, `Date`, `Description`, `Amount`, `Period`, `Household`, `Household Ref`, `Display Name`, `Phone`, `Email`, `Notes`). For `AdminPendingScreen` purge/none messages use `t('adminPending.purged', { count: result.deleted })` and `t('adminPending.noneExpired')`. Inspect `ResidentPendingScreen.tsx` and migrate any visible strings it contains under a new `residentPending.*` block if present (add matching keys to all three files); if it has no user-facing literals, no change is needed.

- [ ] **Step 3: Type-check and run the full suite**

Run: `npx tsc --noEmit && CI=true npx react-scripts test --watchAll=false`
Expected: tsc exit 0; all suites pass. Update any test asserting a moved string to the same English literal.

- [ ] **Step 4: Commit**

```bash
git add ui/react/src/components ui/react/src/i18n/locales
git commit -m "feat(react): i18n remaining screens (notifications, privacy, contact-edit, admin-pending, reservations)"
```

---

### Task 8: Locale parity test + final verification

**Files:**
- Create: `ui/react/src/i18n/parity.test.ts`

- [ ] **Step 1: Write the failing parity test**

Create `src/i18n/parity.test.ts`:
```ts
import bg from './locales/bg.json';
import ru from './locales/ru.json';
import en from './locales/en.json';

function flatKeys(obj: Record<string, unknown>, prefix = ''): string[] {
  return Object.entries(obj).flatMap(([k, v]) =>
    v && typeof v === 'object'
      ? flatKeys(v as Record<string, unknown>, `${prefix}${k}.`)
      : [`${prefix}${k}`],
  );
}

test('bg, ru, en have identical key sets', () => {
  const kbg = flatKeys(bg).sort();
  const kru = flatKeys(ru).sort();
  const ken = flatKeys(en).sort();
  expect(kru).toEqual(kbg);
  expect(ken).toEqual(kbg);
});

test('no translation value is empty', () => {
  for (const [name, res] of [['bg', bg], ['ru', ru], ['en', en]] as const) {
    const flat = flatKeys(res as Record<string, unknown>);
    for (const key of flat) {
      const value = key.split('.').reduce<any>((o, part) => o?.[part], res);
      expect(typeof value === 'string' && value.length > 0).toBe(true);
    }
  }
});
```

- [ ] **Step 2: Run the parity test; fix any key mismatches**

Run: `CI=true npx react-scripts test parity.test --watchAll=false`
Expected: PASS. If it fails, it lists the diverging keys — add the missing key to the deficient locale file(s) with a correct translation, then re-run until green.

- [ ] **Step 3: Full suite + type-check + grep for stragglers**

Run:
```bash
npx tsc --noEmit && CI=true npx react-scripts test --watchAll=false
```
Then confirm no obvious hardcoded English JSX text remains:
```bash
grep -rnoE ">[A-Z][a-z]{3,}" ui/react/src/components ui/react/src/App.tsx | grep -viE "Mui|Icon|Box|Typography|Button|Table|Chip|Alert|Grid|Stack|Paper|Menu|Dialog|Tab|Toolbar|AppBar"
```
Expected: no user-facing English strings (only component/JSX identifiers). Migrate any that appear.

- [ ] **Step 4: Manual smoke (developer-run)**

Rerun `scripts/dev-start.ps1`, load `localhost:3000`. Verify: default UI is Bulgarian; the globe switcher shows on the sign-in page and (after sign-in) the AppBar; switching to Русский / English updates all visible text and MUI widgets; the choice survives a reload. Note the result in the PR.

- [ ] **Step 5: Commit**

```bash
git add ui/react/src/i18n/parity.test.ts
git commit -m "test(react): locale key parity + non-empty value guard"
```

---

## Self-Review

**Spec coverage:**
- react-i18next infra + localStorage detection + BG fallback → Task 1. ✓
- `src/i18n/` with init + three locale files → Tasks 1, 4–7. ✓
- Dynamic MUI locale (bg/ru/en packs) → Task 2 + wired in Task 4. ✓
- LanguageSwitcher in AppBar + SignInPage → Tasks 3–4. ✓
- All strings migrated across 17 components + App.tsx → Tasks 4–7. ✓
- Tests run in English so existing suite passes → Task 1 (setupTests). ✓
- New tests (default lang, switch, persist, both placements, parity) → Tasks 1, 3, 8. ✓

**Placeholder scan:** No TBD/TODO; every locale value and code block is concrete. ✓

**Type consistency:** `makeTheme(lang)`, `muiLocaleFor(lang)`, `SUPPORTED_LANGS`, `Lang`, `LANG_STORAGE_KEY`, and the `harmonia.lang` storage key are used consistently across Tasks 1–4. Locale key names introduced in Tasks 4–7 are the exact keys referenced by the component migrations in the same tasks and validated by the parity test in Task 8. ✓
