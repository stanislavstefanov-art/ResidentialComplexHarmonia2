# Seam Ledger -- reserve-bbq-slot

| # | Handoff | File | Mark | What happened | Gate status | Assumption used? | Owner to harden |
|---|---------|------|------|---------------|-------------|------------------|-----------------|
| 1 | feature → (200 / 300 / 900) identity | `feature.md` → `900-security-review.md` | missing | The feature says "signed-in resident" (AC-6) but no station owns *how* identity/session is established or verified. It is consumed downstream (AC-6 access gate) but never mastered in this slice. Security made it the root residual risk (F1/RR-1). | training-open | yes — "identity/session provided & trustworthy upstream" (A3 / D1 / LA-500-3) | Identity / Security owner |
| 2 | 500 → 800 deployment shape | `500-implementation.md` → `800-infra.md` | under-supply | The implementation plan fixed no concrete stack (no container, manifest, or runtime target). Infra proceeded on a shape-only assumption (stateless scale-to-zero + one managed store) rather than inventing a full deployment spec. | recorded-open (→ `hard-stop` for prod via G-INFRA-1/2) | yes — LA-800-1 (deployment *shape*, not stack) | Engineering + Infra/Ops |
| 3 | 100 → 200 → 700 slot grid | `100-opportunity-brief.md` → `700-data-design.md` | under-supply | The feature never defined the slot grid (duration / count per day / bookable window). Product kept ACs grid-agnostic; Data modelled it as a configurable `SlotGridConfig` (E2) so UC-1 guards whatever grid is chosen. | recorded-open | yes — PA1 / DA-*/ LA-DATA-1 (discrete predefined slots, grid configurable) | Product owner |
| 4 | 200 → 300 cancellation scope | `200-spec.md` → `300-design.md` | clean (bounded scope cut) | Cancellation/release of a held slot was not in the feature; Product took the minimal reading and scoped it **out**, Design gave "Yours" no exit affordance. Recorded rather than silently absorbed. Reopening it adds a concurrency-safe free operation + a hold-theft surface. | recorded-open | yes — G2 (cancellation out of scope) | Product owner |
| 5 | 700/800 → 900 store atomicity | `700-data-design.md` / `800-infra.md` → `900-security-review.md` | clean (dependency surfaced) | The whole no-double-booking guarantee rests on the store genuinely enforcing UC-1 atomically. Handled by construction: ADR-001 makes it a disqualifying store-selection criterion, verified at RC-1/DQ-1/TC-18 on the real store. | training-open (GATE-ARCH-1) | yes — LA-ARCH-2 / LA-500-1 (Option A assumed pending confirmation) | Architecture + Data owner |

**First seam to harden:** Seam #1 — the identity/session source (feature → AC-6). It is the one seam the slice's server-authoritative design cannot self-protect: the residents-only boundary rests entirely on an upstream session assertion this feature consumes but never verifies. It must be owned and proven (real-verifier forge/replay, TC-27/28) before any pilot.
